const HOME_PAGE = "Home — Conduit"
const EDITOR_PAGE = "Editor — Conduit"
const SIGN_IN_PAGE = "Sign in — Conduit"
const SETTINGS_PAGE = "Settings — Conduit"

export default class Constant {

    static get HOME_PAGE() {
        return HOME_PAGE;
    }
    static get EDITOR_PAGE() {
        return EDITOR_PAGE;
    }
    static get SIGN_IN_PAGE() {
        return SIGN_IN_PAGE;
    }
    static get SETTINGS_PAGE() {
        return SETTINGS_PAGE;
    }
}